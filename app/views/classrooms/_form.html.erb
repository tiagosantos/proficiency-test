<%= form_for(@classroom) do |f| %>
  <% if @classroom.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@classroom.errors.count, "error") %> prohibited this classroom from being saved:</h2>

      <ul>
      <% @classroom.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="form-group">
  <div class="col-md-4">
    <%= f.label :student_id %><br>
    <%= collection_select(:classroom, :student_id, @students, :id, :name,{}, {:class=>'form-control form-group'})%>
    <%= f.label :course_id %><br>
    <%= collection_select(:classroom, :course_id, @courses, :id, :name,{}, {:class=>'form-control form-group'})%>
  </div>
</div>
<div class="form-group">
  <div class="col-md-12">
    <br>
    <%= f.submit  class: "btn btn-success form-group-sm" %>
    <%= link_to classrooms_path, :class => "btn btn-danger form-group-sm" do %>
        <i class="fa fa-undo"></i><% end %>
  </div>
  </div>
<% end %>